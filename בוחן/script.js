const button = document.getElementById('showSquareBtn');
        const square = document.getElementById('square');

        button.addEventListener('click', () => {
            square.style.display = 'block'; 
        });